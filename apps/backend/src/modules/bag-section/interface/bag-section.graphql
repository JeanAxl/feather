type Item {
  id: ID!
  name: String!
  description: String!
  quantity: Int!
  weight: Int!
}
type BagSection {
  name: String!
  items: [Item!]!
}

type Query {
  bagSection: BagSection!
}

type Mutation {
  updateBagSection(name: String!): BagSection!
}
